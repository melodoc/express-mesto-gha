const UPDATE_PARAMS = {
  new: true,
  runValidators: true,
};

const EXPIRATION_PERIOD = 3600 * 168;
const SECRET_KEY = 'some-secret-key';

module.exports = {
  UPDATE_PARAMS,
  EXPIRATION_PERIOD,
  SECRET_KEY,
};
